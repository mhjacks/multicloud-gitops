clusterGroup:
  name: group-one
  # Namespace is a dictionary for easier overriding (lists are supported as well)
  namespaces:
    vault:
    openshift-cluster-csi-drivers:
      operatorGroup: true
      targetNamespaces: []
    external-secrets-operator:
      operatorGroup: true
      targetNamespaces: []
    external-secrets:
    config-demo:
    hello-world:
    vp-stakater-reloader:
  # The gitops subscription on spokes gets managed by ACM
  subscriptions:
    openshift-sscsi:
      name: secrets-store-csi-driver-operator
      namespace: openshift-cluster-csi-drivers
      channel: stable
      source: redhat-operators
      sourceNamespace: openshift-marketplace
    eso:
      name: openshift-external-secrets-operator
      namespace: external-secrets-operator
      channel: stable-v1
  argoProjects:
    - hub
    - eso
    - config-demo
    - hello-world
  applications:
    openshift-external-secrets:
      name: openshift-external-secrets
      namespace: external-secrets
      argoProject: eso
      chart: openshift-external-secrets
      chartVersion: 0.0.*
    # Cluster-wide Vault CSI provider + TLS CA ConfigMap (vault ns). SPC lives in charts/all/config-demo (vp-sscsi-spc).
    openshift-sscsi-vault:
      name: openshift-sscsi-vault-group-one
      namespace: vault
      argoProject: hub
      chart: openshift-sscsi-vault
      chartVersion: 0.2.*
      # multisource: chartVersion is Argo targetRevision for this repo (main branch).
      ignoreDifferences:
        - group: ""
          kind: ConfigMap
          name: openshift-sscsi-vault-vault-tls-ca
          namespace: vault
          jsonPointers:
            - /data
          jqPathExpressions:
            - .data
      overrides:
        - name: vault.global.externalVaultAddr
          value: "https://vault-vault.apps.{{ $.Values.global.hubClusterDomain }}"
      syncPolicy:
        automated: {}
        retry:
          limit: 20
        syncOptions:
          - RespectIgnoreDifferences=true
      # SS-CSI workload auth for this spoke clustergroup (ACM clusterGroup group-one; config-demo region-one.example.com).
      # Vault role is <vault_path>-sscsi-<roleSlug> (spoke FQDN = ACM global.clusterDomain). vp-sscsi-spc uses the same
      # prefix as vaultKubernetesMountPath, not this short cluster label. cluster_utils matches rows by group name or FQDN.
      ssCsiWorkloadAuth:
        - serviceAccount: config-demo-sa
          namespace: config-demo
          roleSlug: config-demo-config-demo-sa-config-demo
    config-demo:
      name: config-demo
      namespace: config-demo
      argoProject: config-demo
      path: charts/all/config-demo
      ignoreDifferences:
        - group: ""
          kind: ConfigMap
          name: config-demo-vault-tls-ca
          namespace: config-demo
          jsonPointers:
            - /data
          jqPathExpressions:
            - .data
      syncPolicy:
        automated: {}
        retry:
          limit: 20
        syncOptions:
          - RespectIgnoreDifferences=true
    hello-world:
      name: hello-world
      namespace: hello-world
      argoProject: hello-world
      path: charts/all/hello-world
    vp-stakater-reloader:
      name: vp-stakater-reloader
      namespace: vp-stakater-reloader
      argoProject: hub
      repoURL: https://github.com/mhjacks/vp-stakater-reloader-chart.git
      chartVersion: main
      path: "."
  imperative:
    verbosity: '-vvv'
    extraPlaybookArgs:
      - '-e'
      - 'pattern_dir=/git/repo'
    ansibleDevMode:
      enabled: true
      ansibleCfgContent: |
        [defaults]
        localhost_warning=False
        retry_files_enabled=False
        # Retry files disabled to avoid cluttering CI/CD environments
        interpreter_python=auto_silent
        timeout=30
        library=~/.ansible/plugins/modules:./ansible/plugins/modules:./common/ansible/plugins/modules:/usr/share/ansible/plugins/modules
        roles_path=~/.ansible/roles:./ansible/roles:./common/ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles
        filter_plugins=~/.ansible/plugins/filter:./ansible/plugins/filter:./common/ansible/plugins/filter:/usr/share/ansible/plugins/filter
        collections_path=~/.ansible/collections:/usr/share/ansible/collections
      requirementsFile: requirements.yml
      requirementsContent: |
        ---
        collections:
          - name: git+https://github.com/mhjacks/rhvp.cluster_utils.git,feature/sscsi-vp-proxy-cluster-ca-chart
    # clustergroup-chart replaces the default imperative ClusterRole rules when this is set (see
    # templates/imperative/clusterrole.yaml), so keep the chart default get/list/watch on * plus
    # create/update/patch on ConfigMaps cluster-wide (vault_ss_csi_* route CA ConfigMaps, etc.).
    clusterRoleYaml:
      - apiGroups:
          - '*'
        resources:
          - '*'
        verbs:
          - get
          - list
          - watch
      - apiGroups:
          - ''
        resources:
          - configmaps
        verbs:
          - get
          - list
          - watch
          - create
          - update
          - patch
    # NOTE: We *must* use lists and not hashes. As hashes lose ordering once parsed by helm
    # The default schedule is every 10 minutes: imperative.schedule
    # Total timeout of all jobs is 1h: imperative.activeDeadlineSeconds
    # imagePullPolicy is set to always: imperative.imagePullPolicy
    # For additional overrides that apply to the jobs, please refer to
    # https://validatedpatterns.io/patterns/multicloud-gitops/mcg-imperative-actions/
    jobs:
      - name: hello-world
        # ansible playbook to be run
        playbook: rhvp.cluster_utils.hello_world
        # per playbook timeout in seconds
        timeout: 234
        # verbosity: "-v"
  extraObjects:
    secrets-store-csi-clustercsidriver:
      apiVersion: operator.openshift.io/v1
      kind: ClusterCSIDriver
      metadata:
        name: secrets-store.csi.k8s.io
      spec:
        managementState: Managed
        logLevel: Normal
        operatorLogLevel: Normal
  # Explicitly mention the cluster-state based overrides we plan to use for this pattern.
  # We can use self-referential variables because the chart calls the tpl function with these variables defined
  sharedValueFiles:
    - '/overrides/values-{{ $.Values.global.clusterPlatform }}.yaml'
    # To mirror the "Classic" magic include structure, the clusterGroup would need all of these:
    # sharedValueFiles:
    #   - '/overrides/values-{{ $.Values.global.clusterPlatform }}.yaml'
    #   - '/overrides/values-{{ $.Values.global.clusterPlatform }}-{{ $.Values.global.clusterVersion }}.yaml'
    #   - '/overrides/values-{{ $.Values.global.clusterPlatform }}-{{ $.Values.clusterGroup.name }}.yaml'
    #   - '/overrides/values-{{ $.Values.global.clusterVersion }}-{{ $.Values.clusterGroup.name }}.yaml"
#  To have apps in multiple flavors, use namespaces and use helm overrides as appropriate
#
#    pipelines:
#      name: pipelines
#      namespace: production
#      project: datacenter
#      path: applications/pipeline
#      repoURL: https://github.com/you/applications.git
#      targetRevision: stable
#      overrides:
#      - name: myparam
#        value: myparam
#
#    pipelines_staging:
#    - name: pipelines
#      namespace: staging
#      project: datacenter
#      path: applications/pipeline
#      repoURL: https://github.com/you/applications.git
#      targetRevision: main
#
#   Additional applications
#   Be sure to include additional resources your apps will require
#   +X machines
#   +Y RAM
#   +Z CPU
#    vendor-app:
#      name: vendor-app
#      namespace: default
#      project: vendor
#      path: path/to/myapp
#      repoURL: https://github.com/vendor/applications.git
#      targetRevision: main

#  managedSites:
#    factory:
#      name: factory
#      # repoURL: https://github.com/dagger-refuse-cool/manuela-factory.git
#      targetRevision: main
#      path: applications/factory
#      helmOverrides:
#      - name: foo
#        value: bar
#      clusterSelector:
#        matchExpressions:
#        - key: vendor
#          operator: In
#          values:
#            - OpenShift
