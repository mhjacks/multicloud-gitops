---
version: "2.0"

# This is the actual default
defaultNamespace: 'validated-patterns-secrets'

secrets:
  - name: test-secret
    namespace: overridden-namespace
    fields:
      - name: username
        value: user
