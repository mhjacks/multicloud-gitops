---
global:
  pattern: multicloud-gitops
  options:
    useCSV: false
    syncPolicy: Automatic
    installPlanApproval: Automatic
  secretStore:
    backend: vault
main:
  clusterGroupName: hub
  multiSourceConfig:
    enabled: true
